<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="edu.snapandsave.HomeActivity" >

    <Button
        android:id="@+id/take_photo_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="takePhoto"
        android:text="@string/btn_take_photo" />

    <Button
        android:id="@+id/show_all_photos_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/take_photo_btn"
        android:layout_marginTop="10dp"
        android:onClick="showPhotos"
        android:text="@string/btn_show_all_photos" />

    <!-- <Button
        android:id="@+id/start_recording_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/show_all_photos_btn"
        android:layout_below="@+id/show_all_photos_btn"
        android:layout_marginTop="10dp"
        android:text="@string/start_recording" /> -->

    <!-- <Button
        android:id="@+id/start_recording_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/show_all_photos_btn"
        android:layout_below="@+id/show_all_photos_btn"
        android:layout_marginTop="10dp"
        android:onClick="record"
        android:text="@string/start_recording" /> -->

</RelativeLayout>